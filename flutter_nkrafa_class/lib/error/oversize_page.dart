import 'package:flutter/material.dart';

class OverSizePage extends StatelessWidget {
  const OverSizePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Oversize'),
      ),
    );
  }
}
